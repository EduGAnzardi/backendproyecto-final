<!DOCTYPE html>
<html>
<head>
    <title>Detalles del Carrito</title>
</head>
<body>
    <h1>Carrito ID: {{cart._id}}</h1>
    <ul>
        {{#each cart.products}}
            <li>
                <h2>{{this.product.title}}</h2>
                <p>Precio: ${{this.product.price}}</p>
                <p>Cantidad: {{this.quantity}}</p>
            </li>
        {{/each}}
    </ul>
    <a href="/carts">Volver a la Lista de Carritos</a>
</body>
</html>